<% provide :page_title, 'answers' %>

<p class="mt-3 fs-3"><%= t('pages.create_new_answer') %></p>

<%= render 'form' %>

<%= link_to t("buttons.back_to_question"), question_path(@question), class: 'btn btn-secondary mb-4' %>