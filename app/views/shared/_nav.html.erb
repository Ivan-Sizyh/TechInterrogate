<nav class="navbar pt-3 pb-5">
  <div class="container">
    <div class="navbar-left">
      <%= link_to 'TeachInterrogate', root_path, class: 'logo link-without-decoration' %></li>
      <%= link_to t('nav.vacancies'), root_path, class: 'link link-without-decoration' %></li>
      <%= link_to t('nav.interviews'), interviews_path, class: 'link link-without-decoration' %></li>
    </div>
    <div class="navbar-right">
      <% if user_signed_in? %>
        <li class="dropdown no-bullet text-white">
          <%= link_to current_user.name_or_email, '#',
            class: 'nav-link px-2 dropdown-toggle',
            data: {"bs-toggle": 'dropdown'} %>
          
          <ul class="dropdown-menu">
            <li>
              <%= link_to t('nav.edit_profile'), edit_user_path(current_user),
                class: 'dropdown-item' %>
            </li>

            <li><hr class="dropdown-divider"></li>

            <li>
              <%= link_to t('nav.logout'), session_path,
                class: 'dropdown-item', data: {method: :delete} %>
            </li>
          </ul>
        </li>
      <% else %>
        <%= link_to t('nav.sign_up'), new_user_path, class: 'link link-without-decoration' %></li>
        <%= link_to t('nav.sign_in'), new_session_path, class: 'link link-without-decoration' %></li>
      <% end %>
    </div>
  </div>
</nav>
