<% provide :page_title, 'vacancies' %>

<p class="mt-3 fs-3"><%= t('pages.create_new_vacancy') %></p>

<%= render 'form' %>

<%= link_to t("buttons.back"), root_path, class: 'btn btn-secondary mb-4' %>