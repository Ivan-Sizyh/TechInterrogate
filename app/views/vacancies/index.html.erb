<% provide :page_title, 'vacancies' %>

<%= link_to t('buttons.create_vacancy'), new_vacancy_path, class: 'btn btn-primary mt-4' %>

<h1 class="mt-4 mb-5 fs-3"> <%= t('vacancy.index') %> </h1>
<div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 row-cols-xl-4 g-4 mb-5">
  <%= render @vacancies %>
</div>
