<%= render 'shared/errors', object: @user %>

<%= form_with(model: @user, data: { turbo: false }) do |f| %>
  <div class="mb-3 row">
    <div class="col-sm-2 col-form-label">
      <%= f.label :email, t('registration_page.email') %>
    </div>

    <div class="col-sm-10">
      <%= f.email_field :email, placeholder: t('registration_page.email'), class: 'form-control form-control-lg' %>
    </div>
  </div>

  <div class="mb-3 row">
    <div class="col-sm-2 col-form-label">
      <%= f.label :name, t('registration_page.name') %>
    </div>

    <div class="col-sm-10">
      <%= f.text_field :name, placeholder: t('registration_page.name'), class: 'form-control form-control-lg' %>
    </div>
  </div>

  <div class="mb-3 row">
    <div class="col-sm-2 col-form-label">
      <%= f.label :password, t('registration_page.password') %>
    </div>

    <div class="col-sm-10">
      <%= f.password_field :password, class: 'form-control form-control-lg' %>
    </div>
  </div>

  <div class="mb-3 row">
    <div class="col-sm-2 col-form-label">
      <%= f.label :password_confirmation, t('registration_page.password_confirmation') %>
    </div>

    <div class="col-sm-10">
      <%= f.password_field :password_confirmation, class: 'form-control form-control-lg' %>
    </div>
  </div>

  <% unless @user.new_record? %>
    <div class="mb-3 row">
      <div class="col-sm-2 col-form-label">
        <%= f.label :old_password, t('registration_page.old_password') %>
      </div>

      <div class="col-sm-10">
        <%= f.password_field :old_password, class: 'form-control form-control-lg' %>
      </div>
    </div>
  <% end %>

  <%= f.submit (@user.new_record? ? t('buttons.register') : t('buttons.save')),
    class: 'btn btn-primary btn-lg mt-4' %>
<% end %>